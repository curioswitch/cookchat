name: Renovate Dependencies

on:
  schedule:
    # https://crontab.guru/#5_12_*_*_1
    - cron: "5 12 * * 1"
  workflow_dispatch:

jobs:
  renovate:
    uses: curioswitch/go-build/.github/workflows/export-renovate.yaml@ee957dee825608623a4ead646c7a3a6220a7ffe8
    with:
      github_app_id: "1047503"
      debug: true
    secrets:
      github_app_key: ${{ secrets.CURIOSWITCH_APP_KEY }}
