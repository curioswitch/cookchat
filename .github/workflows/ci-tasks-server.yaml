name: Tasks Server CI
on:
  push:
    branches:
      - main
    paths:
      - common/go/**
      - tasks/server/**
      - tasks/api/go/**
      - .github/workflows/ci-tasks-server.yaml
  pull_request:
    paths:
      - common/go/**
      - tasks/server/**
      - tasks/api/go/**
      - .github/workflows/ci-tasks-server.yaml
  workflow_dispatch:
  workflow_call:

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  ci:
    uses: curioswitch/go-curiostack/.github/workflows/export-go-server-ci.yaml@52897f755daf6e0664134c776e5145362a49795a
    with:
      project_dir: tasks/server
      service: tasks-server
    secrets: inherit
