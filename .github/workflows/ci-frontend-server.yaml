name: Frontend Server CI
on:
  push:
    branches:
      - main
    paths:
      - common/go/**
      - frontend/server/**
      - frontend/api/go/**
      - .github/workflows/ci-frontend-server.yaml
  pull_request:
    paths:
      - common/go/**
      - frontend/server/**
      - frontend/api/go/**
      - .github/workflows/ci-frontend-server.yaml
  workflow_dispatch:
  workflow_call:

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  ci:
    uses: curioswitch/go-curiostack/.github/workflows/export-go-server-ci.yaml@784dcc35a1f2d90421937ca113bd68e42a446020
    with:
      project_dir: frontend/server
      service: frontend-server
    secrets: inherit
