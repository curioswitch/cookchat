name: Crawler Server CI
on:
  push:
    branches:
      - main
    paths:
      - common/go/**
      - crawler/server/**
      - crawler/api/go/**
      - .github/workflows/ci-crawler-server.yaml
  pull_request:
    paths:
      - common/go/**
      - crawler/server/**
      - crawler/api/go/**
      - .github/workflows/ci-crawler-server.yaml
  workflow_dispatch:
  workflow_call:

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  ci:
    uses: curioswitch/go-curiostack/.github/workflows/export-go-server-ci.yaml@bdb1a841449a0fb07f8e4a0282924e588d5ea01c
    with:
      project_dir: crawler/server
      service: crawler-server
      deploy_dev: false
    secrets: inherit
